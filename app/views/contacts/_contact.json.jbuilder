json.extract! contact, :id, :first_name, :Prefix, :first_name, :middle_initial, :last_name, :suffix, :title, :business_email, :personal_email, :add_email, :shared, :phone_1, :Phone_2, :comments, :created_at, :updated_at
json.url contact_url(contact, format: :json)
